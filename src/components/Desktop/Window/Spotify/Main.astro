---
import { spotify } from "/src/data/layout.json";
import Track from "/src/components/desktop/window/spotify/track.astro";

const { year } = Astro.props;

const { tracks, url } = spotify["_" + year];
---

<main>
  {
    tracks.map(function (track, index) {
      const trackProps = {
        number: index + 1,
        track,
        url,
      };
      return <Track {...trackProps} />;
    })
  }
</main>

<style lang="scss">
  @import "/src/styles/mixins";

  main {
    max-height: $width-window;
    overflow: scroll;
    padding: 0.75rem;
  }
</style>
